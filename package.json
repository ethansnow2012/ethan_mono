{
  "name": "monorepo-typescript",
  "version": "1.0.0",
  "description": "A monorepo in TypeScript",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.12.8",
    "ts-node": "^10.9.2"
  },
  "scripts": {
    "apps:react_next": "cd apps/react_next_practice && pnpm run dev",
    "apps:my_three": "cd apps/my_first_three_project_with_nuxt && pnpm run dev",
    "packages:using_core": "cd packages/using_core && pnpm run dev"
  }
}
