{
  "name": "monorepo-typescript",
  "version": "1.0.0",
  "description": "A monorepo in TypeScript",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "typescript": "^5.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.12.8",
    "ts-node": "^10.9.2"
  },
  "scripts": {
    "apps:my_three": "cd apps/my_first_three_project_with_nuxt && pnpm run dev",
    "packages:using_core": "cd packages/using_core && pnpm run dev"
  }
}
